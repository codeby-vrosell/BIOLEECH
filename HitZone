using UnityEngine;

[System.Serializable]
public class HitZone {

    // ============================================================
    // Angle Range
    // ============================================================

    public float minAngle;      // Minimum angle of the hit zone in degrees
    public float maxAngle;      // Maximum angle of the hit zone in degrees

}
